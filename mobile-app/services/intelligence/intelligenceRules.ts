/** Re-exports rule helpers used by UI (priority colors, estimators). */
export {
  buildDestinationPulse,
  buildEvents,
  buildIdealPlan,
  buildImpactFromEvents,
  buildSignals,
  buildWhatChanged,
  buildWhy,
  estimateCrowdLevel,
  estimateMobilityPressure,
  estimateParking,
  priorityColor,
} from './intelligenceEngine';
